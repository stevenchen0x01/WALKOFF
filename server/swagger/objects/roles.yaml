OutputRole:
    type: object
    properties:
      name:
        description: Name of the role
        type: string
        example: administrative_role
      description:
        description: Description of the role
        type: string
        example: A user with this role can access any page and has administrative-level privileges.
      pages:
        description: A list of endpoints that a user with this role can access.
        type: array
        items:
          type: string
          example: /configuration
CreateRole:
    type: object
    required: [name]
    properties:
      name:
        description: Name of the role
        type: string
        example: administrative_role
      description:
        description: Description of the role
        type: string
        example: A user with this role can access any page and has administrative-level privileges.
      pages:
        description: A list of endpoints that a user with this role can access.
        type: array
        items:
          type: string
          example: /configuration
UpdateRole:
    type: object
    properties:
      name:
        description: Name of the role
        type: string
        example: administrative_role
      description:
        description: Description of the role
        type: string
        example: A user with this role can access any page and has administrative-level privileges.
      pages:
        description: A list of endpoints that a user with this role can access.
        type: array
        items:
          type: string
          example: /configuration
